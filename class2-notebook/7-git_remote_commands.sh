# git basic commands

# Clone an existing git repo
git clone <repository>

# Push existing changes to remote repository
git push

# Push existing changes to remote repository
git pull

# Check git remotes
git remote -v

